# 친구들의 타워 디펜스 설계 메모

이 문서는 이 게임이 어떤 게임인지, 어떤 룰로 돌아가는지, 그리고 현재 코드에서 그 룰을 어떤 방식으로 구현하는지 설명한다.

## 게임 목표

`친구들의 타워 디펜스`는 웹 브라우저에서 실행되는 1v1 실시간 타워 디펜스 게임이다. 기본 구조는 Clash Royale 방식에 가깝다. 두 플레이어가 각자 카드로 유닛을 소환하고, 유닛과 타워가 자동으로 싸우며, 상대 킹타워를 먼저 파괴하는 쪽이 승리한다.

## 기본 룰

- 플레이어는 2명이다.
- 플레이어는 로그인해야 메인 화면과 게임에 접근할 수 있다.
- 플레이어는 방을 만들거나 대기 중인 방에 참가한다.
- 방의 첫 번째 플레이어는 아래 진영, 두 번째 플레이어는 위 진영으로 배정된다.
- 한 방에 두 명이 접속하면 경기가 자동으로 시작된다.
- 각 플레이어는 킹타워 1개와 프린세스타워 2개를 가진다.
- 킹타워가 파괴되면 즉시 패배한다.
- 프린세스타워가 남아 있으면 킹타워는 직접 공격 대상이 되지 않는다.
- 킹타워는 처음에는 공격하지 않고, 피해를 받아 각성한 뒤부터 공격한다.
- 정규 경기 시간은 3분이다.
- 3분이 끝났을 때 타워 총 HP가 더 높은 쪽이 승리한다.
- 타워 총 HP가 같으면 서든 데스에 들어간다.
- 정규 경기 마지막 1분부터 타이머 아래 `X2`가 표시되고 엘릭서가 2배 속도로 충전된다.
- 서든 데스 중에도 X2 엘릭서 충전은 유지된다.

## 계정, 로비, 방 룰

- 회원가입과 로그인은 이름과 비밀번호만 사용한다.
- 비밀번호는 bcrypt 해시로 저장하고, 평문은 저장하지 않는다.
- 로그인 성공 후 메인 화면에는 이름, 현재 트로피, 현재 티어가 보인다.
- 메인 화면 내 프로필의 `티어표 보기` 버튼으로 전체 티어표를 볼 수 있고, 내 현재 티어 위치가 강조된다.
- 메인 로비 상단에는 트로피 기준 1~3등 랭킹이 닉네임과 티어로 표시된다.
- 로비 랭킹의 `더 보기`를 누르면 전체 랭킹 화면으로 이동한다.
- 캐릭터 확인 페이지는 각 캐릭터의 설명과 함께 HP, 공격력, 사거리, 공격 주기, 이동속도, 특수 스탯을 표시한다.
- 화면 우측 상단의 테마 버튼으로 라이트 모드와 다크 모드를 전환할 수 있고, 선택한 모드는 브라우저에 저장된다.
- 전투 화면의 경기 시간, 상태, 플레이어 정보, 엘릭서는 오른쪽 여백 패널에 세로로 표시되어 전장을 가리지 않는다.
- 메인 화면의 `덱짜기`에서 카드 8장을 선택해 저장하면 이후 경기마다 저장한 덱 순서대로 손패와 순환 덱이 구성된다.
- `게임 시작`을 누르면 방 만들기와 방 참가 화면으로 이동한다.
- 방 만들기에서는 방 이름과 선택 비밀번호를 입력할 수 있다.
- 방 참가 화면에는 대기 중인 방 목록이 실시간으로 표시된다.
- 비밀번호가 있는 방은 참가할 때 비밀번호가 필요하다.
- 한 방은 최대 2명이며, 2명이 되면 자동으로 전투를 시작한다.
- 경기 종료 후 두 플레이어가 모두 재경기를 누르면 같은 방에서 새 경기를 시작한다.
- 방에 있던 두 플레이어가 모두 나가면 방은 자동으로 삭제된다.

## 트로피와 티어 룰

- 킹타워 파괴 승리: 트로피 +2
- 제한 시간 종료 또는 서든 데스 HP 우위 승리: 트로피 +1
- 패배 2회마다 트로피 -1
- 트로피는 0 아래로 내려가지 않는다.
- 티어는 트로피 수에 따라 자동으로 갱신된다.

| 티어 | 트로피 |
|---|---:|
| 🪨 마이어스 | 0~9 |
| 🥉 브론즈 | 10~29 |
| 🥈 실버 | 30~59 |
| 🥇 골드 | 60~99 |
| 💎 다이아몬드 | 100~149 |
| 👑 마스터 | 150~199 |
| 🏆 챔피언 | 200 이상 |

## 엘릭서와 카드 룰

- 엘릭서는 최대 10까지 충전된다.
- 카드를 사용하면 카드 코스트만큼 엘릭서가 차감된다.
- 현재 카드풀은 14장이다.
- 저장된 덱이 있으면 게임 시작 시 그 8장을 사용하고, 저장된 덱이 없으면 각 플레이어가 14장 카드풀에서 독립적으로 8장을 무작위로 뽑는다.
- 두 플레이어는 서로 같은 카드를 뽑을 수 있다.
- 무작위 덱은 뽑힌 8장의 총 엘릭서 비용이 40 이상 55 이하가 아니면 조건을 만족할 때까지 다시 뽑는다.
- 저장 덱은 정확히 8장의 고유한 플레이 가능 카드인지 서버에서 검증한다.
- 화면에는 손패 4장만 표시된다.
- 카드를 사용하면 다음 카드가 손패로 올라온다.
- 일반 카드는 사용 후 다시 덱 순환에 들어가며, 8장을 모두 사용하면 처음 카드부터 다시 순환한다.
- 박바둑과 조현우가 동시에 손패에 있으면 절친 특성이 발동해 둘 중 하나를 낼 때 8 엘릭서로 둘 다 동시에 출격하고 `절친 특성!` 문구가 전장에 뜬다.
- 꽁호와 대.근.영은 1회용 카드라 한 경기에서 한 번만 사용할 수 있고, 사용 후 덱에서 제거되어 다시 순환하지 않는다.
- 무작위 덱 총합 코스트는 40 이상 55 이하 범위로 검증한다.

## 카드 콘셉트

| 카드 | 코스트 | 역할 | 핵심 규칙 |
|---|---:|---|---|
| 짱가 | 4 | 광역 딜러 | HP 646, 공격력 83, 주기적으로 전방 음파 광역 공격 |
| 빼트맨 | 2 | 힐러 | 가장 가까운 여학생 또는 여학생으로 인식되는 유닛을 따라가며 높은 힐량으로 회복 |
| 박바둑 | 8 | 카오스 딜러 | HP 1300, 낮아진 랜덤 광역 피해, 아군에게도 약한 피해, 조현우와 절친 출격 시 HP/피해량 10% 감소 |
| 꽁호 | 10 | 필드 리셋 | 한 번만 사용 가능하며 모든 유닛 제거 |
| 유신 | 4 | 군단 | HP 50의 8명이 동시에 소환되는 근접 유닛 |
| 지민 | 3 | 단일 폭딜 | 공격력 89, 매우 짧은 공격 전 준비 시간이 있고 피격되어도 취소되지 않음, 유신 공격 시 `인포 가위질!` 문구와 함께 상대 유신 유닛 2~3명 타격 |
| 미토스건휘 | 5 | 변신 딜러 | 기본 공격력 63, HP 50% 이하에서 각성하고 각성 중 공격력 108과 무적 |
| 복숭아 | 2 | 근접 연타 | 빠른 근접 공격 |
| 성주 | 3 | 원거리 딜러 | 키보드를 치며 원거리 공격하고 공격 주기가 10% 느려졌지만 체력이 매우 낮음, 빼트맨 회복 대상 |
| 조현우 | 4 | 근접 단일 딜러 | HP 656, 공격력 88의 단일 근접 공격, 박바둑과 절친 출격 시 HP/피해량 10% 감소 |
| 대.근.영 | 10 | 탱커 소환 + 근접 딜러 | HP 1800, 공격력 90, 전장에 있는 동안 HP 300 광주 탱크를 2초마다 소환하고 한 번만 사용 가능 |
| 김루이 | 4 | 흡혈 부착 딜러 | 적에게 달라붙어 낮은 피해로 체력을 흡수하지만 대상 행동은 막지 않음 |
| 허선 | 8 | 버서커 / 폭발형 딜러 | 평소에는 공격하지 않고 걷기만 하다가 HP 24.3% 이하에서 최대 HP 900으로 완전 회복, 공격력 70과 0.303초 다중 공격으로 폭주 |
| OSJ | 6 | 밀치기형 탱커 | HP 1200, 공격력 20, 전방 적들을 `잡상인들 다 나가!`로 크게 밀쳐 진형을 무너뜨림 |

## 구현 방식

게임의 기준 상태는 백엔드 `server.js`가 관리한다. 클라이언트는 화면을 그리고 플레이어 입력을 서버에 보내는 역할을 한다. 중요한 판정은 서버에서 처리해서 두 플레이어의 화면이 같은 결과를 보게 만든다.

### 백엔드 구현

- Node.js와 Express로 정적 파일 서버를 만든다.
- SQLite에 사용자 이름, 비밀번호 해시, 트로피, 티어, 패배 카운터를 저장한다.
- SQLite 파일은 `DB_PATH`, `DATA_DIR`, Railway Volume의 `RAILWAY_VOLUME_MOUNT_PATH`, 로컬 `data/` 순서로 저장 경로를 정한다.
- Express Session으로 로그인 상태를 유지하고, Socket.io 연결도 같은 세션으로 인증한다.
- Socket.io로 플레이어 접속, 카드 사용, 상태 동기화, 이펙트 이벤트를 주고받는다.
- 서버는 방 목록을 `Map`으로 관리하고, 각 방은 독립된 `game` 객체를 가진다.
- 서버 틱은 50ms마다 실행된다.
- 클라이언트 상태 브로드캐스트는 100ms마다 실행된다.
- 카드 정보는 `CARDS` 상수에 코스트, HP, 데미지, 사거리, 이동속도, 스킬 값을 넣어 관리한다.
- 저장 덱은 `user_decks` 테이블에 카드 ID 배열 JSON으로 저장한다.
- `startMatch(room)`이 플레이어 초기화, 타워 생성, 타이머 시작을 담당한다.
- `tickGame(room)`이 엘릭서 충전, 유닛 업데이트, 타워 공격, 사망 처리, 승리 조건 검사를 담당한다.
- `playCard()`가 손패, 엘릭서, 소환 위치, 1회용 카드 조건을 검증한다.
- `requestRematch()`가 경기 종료 후 두 플레이어의 재경기 동의를 모아 같은 방에서 새 경기를 시작한다.
- 박바둑과 조현우의 절친 특성은 서버에서 손패 조건과 8 엘릭서 비용을 검증한 뒤 두 카드를 한꺼번에 소환하고 전용 이펙트를 보낸다.
- `serializeState()`가 클라이언트로 보낼 최소 상태를 만든다.
- `serializeState()`는 접속자 슬롯을 기준으로 자기 손패만 내려주고 상대 손패는 숨긴다.
- `settleTrophies()`가 경기 종료 후 승패에 따른 트로피, 티어, 패배 카운터를 저장한다.
- 1회용 카드는 서버에서 사용 여부를 기록하고, 사용 후 손패 순환으로 되돌리지 않는다.
- 대.근.영은 전장에 있는 동안 `geunyoungTank` 광주 탱크를 2초마다 소환한다. 본체는 HP 1800과 공격력 90, 광주 탱크는 HP 300을 사용한다.
- 김루이는 적 유닛에게 부착해 지속 피해와 자가 회복을 처리하고, 적 아군에게 공격받으면 떨어진다.
- Railway 배포를 고려해 보안 헤더, CSP, Origin 검사, HTTP/소켓 속도 제한, 비정상 이벤트 차단을 서버에서 처리한다.

### 프론트엔드 구현

- Phaser.js로 게임 화면을 렌더링한다.
- 첫 화면에는 로그인과 회원가입 폼을 보여준다.
- 로그인 후 메인 화면에는 이름, 트로피, 티어와 `게임 시작`, `캐릭터 확인`, `로그아웃` 버튼을 보여준다.
- `게임 시작`은 방 만들기와 방 참가 화면으로 이동한다.
- `캐릭터 확인`은 12명 캐릭터의 비용, 타입, 능력, 외형, 특징을 한국어 카드 그리드로 보여준다.
- `public/src/main.js`의 `BattleScene`이 전체 화면, 입력, HUD, 카드 UI, 유닛/타워 그리기를 담당한다.
- 클라이언트는 `/api/rankings`로 트로피 기준 랭킹을 받아 로비 TOP 3와 전체 랭킹 화면을 렌더링한다.
- 클라이언트는 서버에서 받은 `state`를 기준으로 화면만 갱신한다.
- 카드를 클릭하면 선택 상태가 되고, 자기 소환 구역을 클릭하면 `play-card` 이벤트를 서버로 보낸다.
- 서버에서 받은 `effect` 이벤트로 소환, 공격, 각성, 승천, 서든 데스 같은 연출을 표시한다.
- 꽁호 승천은 빛줄기, 중앙 기도 실루엣, 유닛별 반응 문구, 브라우저 오디오 찬송음으로 표현한다.

## 문서 관리 규칙

- `README.md`에는 프로젝트 구조, 사용 기술, 실행 방법을 적는다.
- `agent.me`에는 게임 룰과 구현 방식을 적는다.
- `update.md`에는 지금까지 구현한 내용과 앞으로 업데이트한 내용을 계속 추가한다.
- 캐릭터 이름, 능력, 밸런스, 역할이 바뀌면 `public/src/main.js`의 캐릭터 확인 페이지 설명도 항상 함께 갱신한다.
- 새 기능을 만들 때는 코드 변경 후 `update.md`도 같이 갱신한다.
